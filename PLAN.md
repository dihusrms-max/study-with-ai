# 웹사이트 향후 개발 및 확장 기획서

## 1. 프로젝트 목적과 개발 흐름

정돈된 정적 웹 구조를 유지하면서 방명록처럼 작은 기능을 점진적으로 확장한다.

```text
요구사항 정의 → 설계 → 개발 → 테스트 → 배포 → LOG.md 기록
```

- **웹:** 정적 HTML, CSS, 브라우저 JavaScript
- **스타일:** `css/global.css`의 전역 토큰과 페이지 전용 CSS 분리
- **Node.js:** 의존성 설치 없이 JavaScript 문법 검사만 수행
- **Python/uv:** 웹과 분리된 학습 환경

## 2. 요구사항 정의 — 방명록

### 기능 요구사항

- `guestbook.html` 페이지를 새로 만든다.
- 공통 헤더에서 모든 페이지와 상호 이동할 수 있어야 한다.
- 닉네임과 내용을 입력하고 방명록을 등록할 수 있어야 한다.
- 등록한 글은 최신순 카드 목록으로 보여야 한다.
- 브라우저를 새로 열어도 같은 브라우저의 등록 글은 유지되어야 한다.

### 입력 제약과 예외 처리

| 항목 | 규칙 |
| --- | --- |
| 닉네임 | 공백만 입력할 수 없고, 최대 10자 |
| 내용 | 공백만 입력할 수 없고, 최대 200자 |
| 등록 실패 | 안내 메시지를 표시하고 기존 목록을 유지 |
| 출력 | 사용자 입력을 HTML로 직접 해석하지 않고 텍스트로 표시 |

### 범위 밖 항목

- 다른 사용자와 방명록을 실시간으로 공유하는 기능
- 관리자 삭제·수정 기능
- 로그인과 계정 관리

이 항목들은 서버 또는 외부 데이터 저장소가 필요한 별도 확장 단계로 관리한다.

## 3. 설계

### 데이터 저장 방식

현재 프로젝트는 정적 사이트이므로 브라우저에서 루트의 `data/guestbook.json`을 직접 수정할 수 없다. 1차 버전은 `localStorage`를 저장소로 사용한다.

- 장점: 서버·Node 패키지·Python 연동 없이 정적 사이트에서 바로 동작
- 한계: 같은 브라우저와 기기에서만 데이터가 보이며, 여러 사용자가 공유할 수 없음

공유형 방명록이 필요해질 때 API와 백엔드를 별도 기획한다. `data/guestbook.json`은 이 단계에서는 샘플 데이터 또는 읽기 전용 목적으로만 검토한다.

### 데이터 구조

```js
{
  id: 1,
  nickname: '작성자',
  content: '방명록 내용',
  createdAt: '2026-08-30T12:00:00.000Z'
}
```

- `id`: 등록 시 생성하는 고유 숫자
- `nickname`: 최대 10자 문자열
- `content`: 최대 200자 문자열
- `createdAt`: 정렬에 사용하는 ISO 8601 문자열

### 화면과 파일 구조

```text
guestbook.html
css/guestbook.css
js/guestbook.js
```

- `guestbook.html`: 입력 폼과 목록 영역
- `css/guestbook.css`: 방명록 화면 전용 UI
- `js/guestbook.js`: 입력 검증, `localStorage` 읽기·쓰기, 최신순 렌더링
- `css/global.css`: 공통 헤더, 테마, 색상·폰트·간격 토큰

모든 페이지의 공통 헤더에 `GUESTBOOK` 메뉴를 같은 순서로 추가한다.

## 4. 개발 마일스톤

- [ ] **요구사항 확정**: 입력 제한, 저장 범위, 삭제 여부를 확인한다.
- [ ] **설계 검토**: `localStorage` 키와 데이터 구조를 확정한다.
- [ ] **개발**: HTML, 페이지 전용 CSS, JavaScript를 구현한다.
- [ ] **통합**: 네 기존 페이지와 새 페이지의 공통 헤더를 동일하게 갱신한다.
- [ ] **테스트**: 정상 등록, 빈 입력, 글자 수 초과, 손상된 저장 데이터, 새로고침 후 목록을 확인한다.
- [ ] **배포 준비**: 공개 범위와 배포 서비스를 결정하고 배포 전 점검을 수행한다.
- [ ] **기록**: 완료·검증된 결과만 `LOG.md`에 누적한다.

## 5. 테스트와 완료 기준

### 최소 테스트 시나리오

1. 유효한 닉네임과 내용을 등록하면 최상단에 표시된다.
2. 닉네임 또는 내용이 공백뿐이면 등록되지 않는다.
3. 글자 수 제한을 넘으면 등록되지 않고 이유가 표시된다.
4. 여러 글은 최신 작성 순서로 표시된다.
5. 새로고침 뒤에도 같은 브라우저에서 목록이 유지된다.
6. `localStorage` 데이터가 손상되었어도 페이지가 멈추지 않고 안내한다.
7. 모든 페이지의 헤더에서 방명록과 기존 기능 페이지로 이동할 수 있다.
8. `npm.cmd run check:js`와 `git diff --check`를 통과한다.

### 완료 기준

- 위 최소 테스트 시나리오를 직접 확인했다.
- JavaScript 문법 검사와 변경 공백 검사를 통과했다.
- 코드와 화면을 검토한 뒤 Git 커밋 대상으로 승인했다.

## 6. 배포 기준

현재 배포 서비스는 아직 선택하지 않았다. 배포 전 다음을 확인한다.

- 공개 가능한 이미지·텍스트·외부 링크만 포함되어 있는가
- 민감한 학습 기록이나 설정 파일이 Git에 포함되지 않았는가
- 모바일 화면과 주요 브라우저에서 핵심 흐름을 확인했는가
- 정적 사이트 배포 서비스의 경로 설정이 상대 링크와 호환되는가

## 7. 기록 규칙

- `PLAN.md`: 앞으로 구현할 요구사항, 설계, 테스트, 배포 기준
- `LOG.md`: 실제로 완료하고 검증한 핵심 변경, 버그 원인, 해결과 회고
- Git 커밋: 승인된 코드와 문서 변경 단위
