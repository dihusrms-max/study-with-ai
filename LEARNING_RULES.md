# 학습 기록 및 지원 공통 규칙

## 1. Python 학습 지원 방식

Python 학습 질문에서는 정답 코드부터 제공하지 않는다.

1. 문제를 작은 단계로 나눈다.
2. 필요한 개념과 접근 방향을 힌트로 제공한다.
3. 개념을 확인할 수 있는 작은 실행 예제와 부분 코드를 제공할 수 있다.
4. 사용자가 직접 작성한 코드를 검토하고, 문제 원인·수정 위치·수정 예시를 설명한다.
5. 막힌 부분에는 힌트, 뼈대 코드, 최소 동작 예제 순서로 도움의 수준을 높인다.
6. 사용자가 명시적으로 요청하거나 충분히 시도한 뒤에도 해결되지 않으면 전체 정답과 이유를 설명한다.
7. 설명이나 정답 뒤에는 직접 다시 해볼 수 있는 짧은 변형 문제를 제안한다.

## 1-1. 맞춤형 코드 보조 기준

사용자는 직접 시도하는 학습을 우선하되, 필요할 때 예제 코드와 구현 보조를 받는 방식을 선호한다. 질문의 목적과 막힌 정도에 따라 다음과 같이 지원한다.

| 상황 | 기본 지원 방식 |
| --- | --- |
| 개념·문법 질문 | 핵심 설명, 작은 실행 예제, 다음에 직접 해볼 한 단계 |
| 실습 문제 | 목표·입출력 예시·단계별 힌트 후 사용자 구현 |
| 코드 오류·리뷰 | 원인, 수정할 위치, 부분 수정 예시, 이유 |
| 구현이 오래 막힌 경우 | 뼈대 코드 또는 최소 동작 예제로 다음 단계를 확인 |
| 전체 구현을 요청하거나 빠른 진행이 필요한 경우 | 전체 코드, 핵심 이유, 검증 방법, 짧은 변형 과제 |

HTML·CSS·JavaScript 프로젝트 작업에서는 파일 구조, 구현 예시, 검증 방법을 더 적극적으로 제안한다. Python을 비롯한 학습 실습에서는 사용자의 직접 시도를 우선하되, 학습 흐름이 끊기지 않도록 위 기준에 따라 보조 강도를 조절한다.

## 1-2. 난이도와 창의 응용 기준

문제를 제안할 때 난이도와 보조 강도를 구분한다. 난이도는 문제 해결에 필요한 개념의 복잡도이고, 보조 강도는 사용자가 받는 힌트와 코드 도움의 수준이다.

| 난이도 | 기준 |
| --- | --- |
| 하 | 배운 개념을 그대로 사용하는 짧은 확인 문제 |
| 중 | 조건을 조합하거나 기존 코드를 수정하는 문제 |
| 상 | 요구사항을 해석하고 구조·예외 처리·여러 개념을 스스로 선택하는 문제 |
| 창의 응용 | 같은 개념을 사용자의 프로젝트·관심 주제에 새 기능 또는 다른 방식으로 적용하는 과제 |

기본 학습 흐름은 `하 1개 → 중 1개 → 필요할 때 상 또는 창의 응용 1개`로 구성한다. 상 난이도와 창의 응용은 매번 강제하지 않으며, 핵심 개념을 직접 구현할 수 있는 수준에 도달했을 때 제안한다.

난이도가 높아져도 보조는 한 번에 정답을 제공하지 않고 다음 단계로 공개한다.

1. 문제 이해 힌트
2. 사용할 개념 힌트
3. 코드 구조 예시
4. 부분 코드 또는 최소 동작 예제
5. 전체 해설

`Learning Python/` 폴더에는 재현할 가치가 있는 실습 코드, 중요한 오류 해결 기록, 장기 복습용 Markdown을 저장한다. 모든 일일 질문이나 학습 내용을 자동으로 파일로 기록하지 않는다.

하루 학습의 핵심 내용은 사용자가 "오늘 일일보고 기록해줘" 또는 "[캠프 일일 보고]"라고 요청했을 때만 선별하며, 다음 세 가지를 중심으로 기존 Notion `일일 학습 기록` 페이지에 정리한다.

1. 오늘 새롭게 이해한 것
2. 아직 이해하지 못했거나 애매한 것
3. 직접 해본 것

## 2. 다른 언어 학습 기록 방식

다른 프로그래밍 언어의 학습 폴더 또는 Markdown 기록 파일을 새로 만들기 전에는 반드시 사용자 승인을 받는다.

승인 요청에는 다음 내용을 포함한다.

- 생성할 폴더 또는 파일 이름
- 기록 목적
- 적용할 학습 지원 방식
- 기본 기록 양식

승인 전에는 새 언어용 학습 폴더나 기록 파일을 생성하지 않는다.

## 3. Git 관리 및 공개 범위

학습 기록은 모두 자동으로 Git에 추가하지 않는다. 재현 가능한 코드, 오류 해결 기록, 학습 규칙, 프로젝트 회고와 기획서는 Git 관리 대상으로 검토한다.

개인 감정, 민감한 정보, 계정 정보, 임시 메모와 미완성 초안은 Git 관리 대상에서 제외하는 것을 우선 검토한다.

학습 기록에 대해 `.gitignore` 규칙을 새로 추가하거나 변경해야 할 때는 반드시 사용자에게 다음 내용을 먼저 제안하고 승인을 받는다.

- 제외할 폴더 또는 파일 경로
- 제외하는 이유와 공개 범위
- 기존 학습 기록에 미치는 영향

사용자 승인 전에는 `.gitignore`을 변경하지 않는다.

## 4. 주간 학습 리뷰와 예약 원칙

일일 학습 기록을 바탕으로 매주 학습 방향을 점검한다.

```text
Codex에서 자유 학습·실습 → 중요한 내용만 Notion 일일 학습 기록에 선별 저장
→ 최근 7일 내용 분석 → 주간 리뷰 초안 → 사용자 검토·승인 → 캠프 Weekly Review 기록
```

- 일일 기록은 Notion `일일 학습 기록` 페이지를 기준으로 하며, 모든 대화를 자동으로 기록하지 않는다.
- 매주 월요일 오전 7시에 최근 7일 기록을 검토하는 주간 리뷰 초안을 준비한다.
- 주간 리뷰에는 새롭게 이해한 내용, 실제로 할 수 있게 된 것, 막힌 지점, Weakness Backlog(✅·🟡·🔴), 다음 주 우선순위 3개, Active Recall 또는 Weekly Test, Weekly Mini Output을 포함한다.
- 예약 실행은 주간 리뷰의 초안만 만들며, 학습 방향과 Notion 기록을 자동으로 확정하지 않는다.
- 개인 Notion은 주간 예약의 대상과 권한에서 제외한다.
- 비즈니스 Notion `캠프 Weekly Review` 페이지에 주차별 리뷰를 누적한다.
- 실제 예약 전에는 실행 시간, 저장 위치, 필요한 권한을 사용자에게 다시 확인하고 승인을 받는다.

## 5. Markdown 원본과 외부 기록 반영

학습 기록이나 회고를 요청받으면 외부 서비스에 먼저 저장하지 않고, 프로젝트 안의 Markdown 원본을 먼저 작성한다.

| 사용자 요청 | Markdown 원본 | 외부 반영 원칙 |
| --- | --- | --- |
| `노션용 기록해줘` | `notion/YYYY-MM-DD-주제.md` | 기본은 Markdown만 작성하며, `노션에 반영해줘` 또는 별도 승인 뒤에만 Notion에 추가한다. |
| `Velog용 기록해줘` | `velog/YYYY-MM-DD-주제.md` | Velog에 자동 게시하지 않고 복사·붙여넣기용 원고만 작성한다. |
| `일일보고 기록해줘` | `notion/YYYY-MM-DD-daily-learning.md` | 새롭게 이해한 것·아직 애매하거나 복습할 것·직접 해본 것의 세 항목으로 작성한 뒤 승인 시 Notion에 반영한다. |
| `주간 회고 기록해줘` | `notion/YYYY-MM-DD-weekly-review.md` | 주간 리뷰 초안을 작성한 뒤 승인 시 `캠프 Weekly Review`에 반영한다. |

Markdown 원본은 외부 서비스의 수정·삭제·연동 실패와 무관하게 보존하는 기준 문서다.

Markdown 학습 기록을 Git에 포함할지는 별도로 검토한다. 재현 가능한 기술 기록·학습 규칙·프로젝트 회고는 Git 관리 후보이며, 개인 정보·감정 기록·계정 정보·미완성 메모는 기본적으로 커밋 대상에서 제외한다. `.gitignore` 변경은 기존 승인 절차를 따른다.
