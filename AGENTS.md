# 작업 시작 전 필수 확인

작업 또는 답변 전에 프로젝트 루트와 작업 대상 폴더의 관련 지시 문서를 확인하고 따른다.
학습 기록, Notion, Velog, 일일보고, 주간 회고와 관련된 요청에서는
`rules_prompt.md`, `LEARNING_RULES.md` 및 관련 `.md` 문서를 반드시 확인한다.

데이터 분석·Pandas 학습 관련 요청에서는 다음 기준 문서를 먼저 확인한다.

- `Learning Python/데이터분석-기획가이드.md`
- `Learning Python/Pandas-명령어-사용가이드.md`

`데이터분석-기획가이드.md`는 분석 목적·질문·범위·지표·한계의 기준 문서로 사용하고,
`Pandas-명령어-사용가이드.md`는 컬럼 선택·추가·삭제·정제·집계·변환 명령어의 기준 문서로 사용한다.

새로운 데이터 분석 학습 내용이 생기면 다음 갱신 규칙을 따른다.

- 분석 목적·질문·지표·범위·한계는 `데이터분석-기획가이드.md`에 반영한다.
- 컬럼 선택·추가·삭제·정제·집계·변환 명령어는 `Pandas-명령어-사용가이드.md`에 반영한다.
- 기존 내용과 중복되면 새 항목을 만들지 않고 기존 항목을 보완한다.
- 기존 설명과 충돌하면 이전 설명을 수정하고 변경 이유를 변경 기록에 남긴다.
- 일회성 오류나 단순 질문은 자동으로 추가하지 않고, 재사용 가능하거나 프로젝트에 적용할 수 있는 내용만 반영한다.
- 문서를 갱신할 때는 현재 내용을 먼저 확인하고, 코드 예시를 검토한 뒤 날짜와 변경 내용을 변경 기록에 추가한다.

## Data Inspector & Merge Advisor 작업 규칙

`Data_Inspector_Merge_Advisor_V0.1.md`를 Data Inspector & Merge Advisor 프로젝트의
기능 명세 및 구현 기준 문서로 사용한다.
이 규칙은 Data Inspector & Merge Advisor의 구현·데이터 결합 작업에 적용하며,
단순 학습 질문이나 일회성 분석에는 필요한 부분만 선택적으로 적용한다.

### 작업 전 확인

데이터 검사·결합·분석 기능을 구현하거나 수정하기 전에 다음 문서를 확인한다.

- `Data_Inspector_Merge_Advisor_V0.1.md`
- `Learning Python/데이터분석-기획가이드.md`
- `Learning Python/Pandas-명령어-사용가이드.md`

### 구현 원칙

- 분석 목적과 사용자의 요구사항을 먼저 확인한다.
- 원본 데이터는 직접 수정하지 않는다.
- 원본 데이터의 파일 형식, 시트, shape, 컬럼, dtype, 결측치, 중복, 공백을 먼저 검사한다.
- 데이터 결합 전 각 데이터의 Grain과 Key 후보를 확인한다.
- Key 후보, Grain 후보, 결합 관계는 자동으로 확정하지 않고 근거와 위험을 함께 제시한다.
- 검사 함수는 원본과 DataFrame을 변경하지 않고 진단 결과와 추천 사항만 반환한다.
- 정제·변환·결합 작업은 검사 단계와 분리한다.
- 추천과 실행을 분리하며, 중요한 변경 작업은 사용자 승인 후 실행한다.
- `merge()` 실행 후 행 수 변화, 미매칭 Key, 중복 증가, N:N 관계, Grain 불일치를 검증한다.
- 결측치 삭제, 이상치 삭제, 컬럼 삭제 및 원본 변경을 자동으로 수행하지 않는다.
- 처리 전·후 데이터 shape와 주요 변경 내용을 기록한다.

### 사용자 승인 대상

다음 작업은 실행 전에 사용자 승인을 받는다.

- 원본 파일 변경
- 결측치·이상치·중복 데이터 삭제
- 컬럼 삭제
- 결과 행 수가 크게 변할 수 있는 `merge()` 실행
- 외부 서비스 반영
- 파일 대량 생성 또는 삭제

단순 조회, 구조 검사, 검증, 리포트 생성은 승인 없이 수행할 수 있다.

### 권장 작업 흐름

```text
INSPECT
→ VALIDATE
→ DEFINE
→ PLAN
→ 사용자 승인
→ EXECUTE
→ VERIFY
→ LOG
```

### V0.1 구현 범위

우선 다음 기능을 구현한다.

- CSV 및 일반 XLSX 파일 검사
- Excel 시트 목록과 시트별 구조 확인
- shape, columns, dtype 확인
- 결측치, 중복, 공백 검사
- 기본 분포 및 숫자 범위 확인
- 날짜형 변환 후보 탐지
- Key 후보 및 주의사항 출력
- 검사 결과와 추천 작업 출력

다음 항목은 V0.1 구현 범위에서 제외하며, 후속 버전에서 추가할 경우
프로젝트 명세서와 roadmap을 먼저 갱신한다.

- 완전 자동 Grain 확정
- 완전 자동 Key 확정
- 자동 결측치·이상치 삭제
- 자동 분석 방법 확정
- 자동 ML 모델 선택
- 완전 자동 데이터 분석
- GUI/Web 서비스 구현

### Transformation Log

중요한 데이터 변환과 결합 과정은 다음 항목을 포함해 기록한다.

```text
date
operation
input
output
rows_before
rows_after
columns_changed
reason
approval
validation_result
```

### 문서 갱신 위치

- 분석 목적·질문·지표·범위·한계:
  `Learning Python/데이터분석-기획가이드.md`
- Pandas 컬럼 선택·정제·집계·변환·결합 명령어:
  `Learning Python/Pandas-명령어-사용가이드.md`
- 시스템 동작 원칙과 안전 규칙: `AGENTS.md`
- 기능 설계·테스트·roadmap: `Data_Inspector_Merge_Advisor_V0.1.md`

새로운 내용을 추가할 때는 기존 문서와의 중복 및 충돌 여부를 먼저 확인하고,
재사용 가능하거나 프로젝트에 적용할 수 있는 내용만 반영한다.
