# 협업 안내

## 작업 흐름

1. `docs/team/decision-log.md` 또는 `experiments/README.md`에 실험을 등록한다.
2. `exp/<번호>-<설명>` 브랜치에서 작업한다.
3. 공통 채점과 제출 형식 검증을 실행한다.
4. 점수·오탐·미탐·환경 정보를 PR에 기록한다.
5. 리뷰 승인 후에만 `main`에 병합한다.

## 후보 코드 배치

개인 작업물은 `candidates/<이름>/`에 보존한다. 후보 원본은 직접 수정하지 않고, 개선 내용은 실험 브랜치에서 `main/`으로 이식한다.

현재 저장소의 기존 `src/`, `tools/`, `configs/`는 1차 통합 전까지 보존한다. 실제 제출 파이프라인이 확정되면 그때 `main/`으로 이동하고, 이동 전후 dev 점수를 비교한다.
