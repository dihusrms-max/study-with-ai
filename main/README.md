# main 제출 파이프라인

이 폴더에는 항상 하나의 제출 가능한 파이프라인만 둔다.

- 직접 커밋하지 않는다.
- 실험 브랜치의 검증된 변경만 PR로 병합한다.
- 제출 파일은 태그의 코드에서 빌드한다.
- 공통 입출력 계약은 `docs/team/data-contract.md`를 따른다.

현재 기존 작업 코드는 통합 전까지 원래 위치에 보존한다. 첫 기준 후보가 선정된 뒤 이동 전후 점수를 비교하여 이 폴더에 배치한다.
