# Data Inspector & Merge Advisor

> **Status:** V0.1 설계 승인\
> **목적:** 데이터 구조와 품질을 Python으로 먼저 진단하고, 사용자의 분석
> 요구사항을 정의한 뒤 안전한 데이터 결합·분석 방식을 추천한다.

------------------------------------------------------------------------
@
## 1. 프로젝트 배경

실제 분석 데이터는 처음부터 분석하기 좋은 형태로 제공되는 경우가 드물다.

-   CSV 인코딩이 다를 수 있다.
-   Excel은 여러 시트와 복잡한 헤더를 가질 수 있다.
-   컬럼명과 문자열에 공백이 존재할 수 있다.
-   숫자·날짜가 `object`로 읽힐 수 있다.
-   결측치와 중복이 존재한다.
-   같은 의미의 값이 서로 다른 표기로 저장될 수 있다.
-   여러 파일의 분석 단위(Grain)가 다를 수 있다.
-   잘못된 Key로 병합하면 N:N 관계가 발생해 행이 폭증할 수 있다.
-   사용자가 분석 목적이나 세부 지시를 처음부터 명확히 정의하지 못할 수
    있다.

따라서 원본 전체를 곧바로 AI에게 전달하기보다 **Python이 객관적인 구조와
품질을 먼저 검사하고, AI에는 분석에 필요한 최소한의 신뢰 가능한 정보만
전달하는 방식**을 사용한다.

------------------------------------------------------------------------

## 2. 핵심 철학

좋은 AI 분석의 입력은 다음 네 가지로 구성한다.

``` text
Purpose
+ Detailed Requirements
+ Reliable Data Description
+ Constraints
        ↓
High Quality AI Analysis
```

### 역할 분리

**Python이 담당할 것**

-   파일 형식 및 인코딩 확인
-   Excel 시트 구조 확인
-   shape / columns / dtype
-   결측치와 중복
-   공백 및 표기 문제
-   고유값과 값 범위
-   Key 후보 탐색
-   Key 중복률
-   컬럼 구조 비교
-   병합 전후 행 수와 매칭률 검증

**AI가 담당할 것**

-   데이터 의미 해석
-   Grain 후보 검토
-   Key 적절성 검토
-   사용자 요구사항 구체화
-   결합 전략 제안
-   분석 방법 및 시각화 제안
-   위험 요소 설명
-   추가 데이터 필요성 판단

> **원칙:** 사실은 Python이 계산하고, 의미와 전략은 AI와 사용자가
> 판단한다.

------------------------------------------------------------------------

## 3. 전체 Workflow

``` text
파일 입력
   ↓
[1] INSPECT
파일 / 시트 / 구조 / 품질 자동 진단
   ↓
[2] VALIDATE
결측 / 중복 / 공백 / dtype / Key / Grain 후보 검토
   ↓
[3] DEFINE
사용자의 분석 요구사항 정의
   ↓
[4] PLAN
전처리 / 결합 / 분석 Pipeline 추천
   ↓
사용자 승인
   ↓
[5] EXECUTE
승인된 작업 수행
   ↓
[6] VERIFY
병합 결과 / 데이터 손실 / 매칭률 검증
   ↓
LOG
모든 변경 이력 기록
```

------------------------------------------------------------------------

# 4. Requirement Builder

데이터가 있어도 분석 목적을 모르면 좋은 분석 계획을 만들기 어렵다.

따라서 사용자가 직접 완벽한 프롬프트를 작성하도록 요구하지 않고 시스템이
요구사항 정의를 지원한다.

## 4.1 세 가지 사용 방식

### Quick Mode --- 잘 모르겠음

데이터 구조를 기반으로 표준적인 분석 방향을 추천한다.

예:

``` text
현재 데이터에서 가능한 분석

1. 현황 분석
2. 집단/지역/항목 비교
3. 변수 간 관계 분석
4. 시간에 따른 변화
5. 이상값 탐색
6. 예측 가능성 검토
```

추천에는 반드시 **추천 이유**를 표시한다.

### Guided Mode --- 질문 기반

사용자 선택에 따라 필요한 질문만 단계적으로 제공한다.

``` text
무엇을 알고 싶은가?
↓
현황 / 비교 / 관계 / 변화 / 예측 / 잘 모르겠음
```

예를 들어 `관계`를 선택했을 때만 다음 질문을 한다.

``` text
어떤 변수와 어떤 변수의 관계를 보고 싶은가?
```

불필요한 질문을 처음부터 모두 하지 않는다.

### Direct Mode --- 직접 설정

숙련된 사용자는 직접 요구사항을 정의한다.

``` text
목적: 이용량과 혼잡도의 관계
기간: 2026-04 ~ 2026-06
분석 단위: 호선 × 역
지표: 총 이용객 / 출근시간 평균 혼잡도
분석 방법: Spearman
```

------------------------------------------------------------------------

# 5. Analysis Requirement 출력

요구사항 정의가 끝나면 다음과 같은 명세를 생성한다.

``` text
[ANALYSIS REQUIREMENT]

목적
지하철 이용량과 혼잡도의 관계 분석

대상 기간
2026-04 ~ 2026-06

분석 단위
호선 × 역

주요 지표
총 이용객 수
출근시간 평균 혼잡도

필요 데이터
월별 승하차 데이터
혼잡도 데이터

결합 Key 후보
호선명 + 역명

필요 전처리
역명 공백 제거
호선 표기 통일
Grain 통일

분석 후보
EDA
Scatter Plot
Spearman Correlation

최종 산출물
상관계수
p-value
상위 혼잡역
시각화
해석
```

사용자가 이 명세를 확인한 뒤 분석을 진행한다.

------------------------------------------------------------------------

# 6. Data Inspector --- V0.1

## 목표

``` python
report = inspect_file("data.xlsx")
```

한 줄로 파일의 기본적인 구조와 품질을 확인한다.

## V0.1 검사 대상

-   CSV / XLSX 구분
-   CSV 인코딩
-   Excel 시트 목록
-   각 시트 shape
-   컬럼명
-   컬럼명 앞뒤 공백
-   dtype
-   결측 개수와 비율
-   완전 중복 행
-   문자열 앞뒤 공백
-   고유값 개수
-   숫자형 min / max 등 기초 통계
-   날짜형 변환 후보
-   Key 후보
-   주의사항
-   추천 작업

## 출력 예시

``` text
==================================================
DATA INSPECTION REPORT
==================================================

FILE
subway.xlsx

TYPE
Excel

SHEETS
4

[202604]

SHAPE
18,430 × 7

MISSING
12

DUPLICATE ROWS
0

COLUMN
사용일자       object
호선명         object
역명           object
승차총승객수    int64
하차총승객수    int64

WARNING

- 사용일자: datetime 변환 후보
- 역명: 앞뒤 공백 23건
- 컬럼명 공백 발견

RECOMMENDATION

1. 컬럼명 strip 검토
2. 역명 strip 검토
3. 사용일자 datetime 변환 검토
4. Key 후보 검증
```

------------------------------------------------------------------------

# 7. 데이터 결합 관점

데이터 결합 전에 가장 먼저 확인할 것은 **한 행의 의미(Grain)** 이다.

예:

``` text
승하차 데이터
Grain = 날짜 × 호선 × 역

혼잡도 데이터
Grain = 요일 × 호선 × 역 × 시간대
```

공통 컬럼이 존재한다는 이유만으로 바로 `merge()`하지 않는다.

## 결합 판단 순서

``` text
최종 분석 목적
↓
목표 DataFrame의 Grain
↓
각 원본의 현재 Grain
↓
필요한 Key
↓
표기/타입 정제
↓
Key 중복 확인
↓
1:1 / 1:N / N:1 / N:N 관계 확인
↓
필요 시 groupby / agg
↓
concat / merge
↓
병합 결과 검증
```

------------------------------------------------------------------------

# 8. Pandas 함수를 목적과 연결해서 학습

함수를 먼저 외우지 않는다.

``` text
문제
↓
데이터 구조
↓
필요한 작업
↓
Pandas 함수
```

  하고 싶은 일            주요 기능
  ----------------------- ---------------------------------------------
  데이터 구조 확인        `head`, `sample`, `shape`, `info`, `dtypes`
  결측 확인               `isna`, `isnull`
  중복 확인               `duplicated`
  값 종류 확인            `unique`, `nunique`, `value_counts`
  공백 정리               `str.strip`
  숫자 변환               `pd.to_numeric`
  날짜 변환               `pd.to_datetime`
  같은 구조의 파일 연결   `concat`
  분석 단위 변경          `groupby`, `agg`
  서로 다른 정보 연결     `merge`
  병합 관계 검증          `merge(validate=...)`
  구조 변경               `pivot`, `pivot_table`, `melt`

------------------------------------------------------------------------

# 9. Merge Planner --- 향후 확장

여러 데이터가 들어오면 다음 정보를 비교한다.

``` python
compare_files(files)
```

## 검사 항목

-   컬럼 일치율
-   dtype 일치 여부
-   공통 컬럼
-   공통 Key 후보
-   Key 중복률
-   concat 가능성
-   merge 가능성
-   1:1 / 1:N / N:1 / N:N 후보
-   Grain 불일치 가능성
-   병합 위험성

## 예시

``` text
승하차 ↔ 혼잡도

COMMON KEY CANDIDATE
호선명 + 역명

KEY DUPLICATION
승하차: HIGH
혼잡도: HIGH

RELATIONSHIP CANDIDATE
MANY TO MANY

MERGE RISK
HIGH

REASON
양쪽 데이터에서 동일 Key가 반복됨.

RECOMMENDATION
Grain 확인
→ 필요한 수준으로 집계
→ Key 재검증
→ merge
```

------------------------------------------------------------------------

# 10. Post-Merge Validation

`merge()`가 실행됐다고 결합이 성공한 것은 아니다.

반드시 병합 전후를 비교한다.

``` text
LEFT
50,000 rows

RIGHT
5,000 rows

MERGED
128,430 rows

WARNING
병합 후 행 수 156% 증가

CHECK
- Key 중복
- Many-to-Many 관계
- Grain 불일치
```

추후 다음 항목도 검사한다.

-   병합 전후 행 수
-   매칭 성공률
-   미매칭 Key
-   중복 증가
-   데이터 손실
-   예상 관계와 실제 관계 차이

------------------------------------------------------------------------

# 11. Transformation Log

모든 중요한 변경을 기록한다.

``` text
[DATA TRANSFORMATION LOG]

01. 컬럼명 공백 제거
변경 컬럼: 2개

02. 역명 공백 제거
변경 행: 23

03. 월별 데이터 concat
3 files → 148,231 rows

04. 역 단위 집계
148,231 → 624 rows

05. 혼잡도 merge
624 → 617 matched rows

미매칭
7개 역
```

------------------------------------------------------------------------

# 12. 자동화 안전 원칙

## 자동으로 확정하지 않는다

다음과 같이 표현한다.

``` text
Key 후보
Grain 후보
날짜 변환 후보
Merge 후보
```

다음처럼 확정적으로 표현하지 않는다.

``` text
Key = 역명
Grain = 역
```

## 원본을 수정하지 않는다

권장 데이터 흐름:

``` text
RAW
↓
PROCESSED
↓
MERGED
↓
ANALYSIS
```

예:

``` text
data/
├── raw/
├── processed/
└── output/
```

## 추천과 실행을 분리한다

``` text
Detect
↓
Explain
↓
Recommend
↓
User Approval
↓
Execute
↓
Verify
↓
Log
```

------------------------------------------------------------------------

# 13. AI 사용 원칙

AI에게 대용량 원본 데이터를 무조건 전달하지 않는다.

Python이 먼저 다음과 같은 구조화된 정보를 만든다.

``` json
{
  "dataset": "subway",
  "shape": [50000, 7],
  "grain_candidate": ["date", "line", "station"],
  "key_candidate": ["date", "line", "station"],
  "missing_rate": 0.001,
  "common_keys": ["line", "station"],
  "merge_relationship_candidate": "many_to_many",
  "merge_risk": "high"
}
```

AI에는 다음을 전달한다.

``` text
분석 목적
+
세부 요구사항
+
데이터 구조 요약
+
데이터 품질 진단
+
제약조건
```

필요할 때만 최소한의 샘플 데이터를 추가한다.

------------------------------------------------------------------------

# 14. 추천 시스템의 출력 원칙

단순히 추천만 하지 않는다.

잘못된 예:

``` text
Spearman 사용
```

권장:

``` text
추천 후보
Spearman 상관분석

이유
두 숫자형 변수의 단조 관계 분석 목적

현재 상태
강한 왜도와 이상치 존재

장점
순위 기반으로 이상치와 비정규 분포의 영향을 상대적으로 덜 받음

주의
관계의 형태와 산점도를 함께 확인해야 함

대안
Pearson 조건 충족 여부 추가 검토
```

즉 모든 중요한 추천은 가능하면 다음을 포함한다.

``` text
Recommendation
+ Reason
+ Condition
+ Risk
+ Alternative
```

------------------------------------------------------------------------

# 15. 현재 알려진 취약점

### Grain 자동 판별

완전 자동 판별하기 어렵다.

**대응:** 후보 + 근거 + 사용자 확인.

### Key 자동 판별

유일성이 높다고 반드시 의미적으로 올바른 Key는 아니다.

**대응:** uniqueness와 의미 검토를 분리.

### 컬럼 의미

컬럼명만으로 단위와 의미를 완전히 알 수 없다.

**대응:** 샘플값, 범위, 메타데이터, 사용자 설명 활용.

### 복잡한 Excel

다중 헤더, 병합 셀, 제목 행, 합계 행, 한 시트의 여러 표 등이 존재할 수
있다.

**대응:** V0.1에서는 일반적인 표 형태를 우선 지원하고 이후 확장.

### 과도한 질문

요구사항 정의가 설문처럼 길어질 수 있다.

**대응:** 필요한 질문만 단계적으로 제공.

### AI의 과신

AI가 부적절한 분석법이나 결합 방법을 자신 있게 추천할 수 있다.

**대응:** 근거·조건·위험·대안을 함께 출력하고 중요한 작업은 사용자 승인
후 실행.

------------------------------------------------------------------------

# 16. V0.1 범위

## 반드시 구현

-   CSV
-   일반적인 XLSX
-   시트 확인
-   shape
-   columns
-   dtype
-   결측
-   중복
-   공백
-   기본 분포
-   Key 후보
-   경고
-   추천 작업

## V0.1에서 제외

-   자동 결측치 삭제
-   자동 이상치 삭제
-   완전 자동 Grain 확정
-   모든 Excel 형태 지원
-   자동 ML 모델 선택
-   AI 완전 자동 분석
-   GUI/Web 서비스
-   임의의 단일 데이터 신뢰도 점수

------------------------------------------------------------------------

# 17. V0.1 성공 기준

단순히 코드가 실행되는 것을 성공으로 판단하지 않는다.

실제 데이터 파일을 넣었을 때 다음 질문에 답할 수 있어야 한다.

1.  수작업 구조 파악보다 시간이 줄었는가?
2.  사람이 놓칠 수 있는 문제를 발견했는가?
3.  경고의 이유를 이해할 수 있는가?
4.  잘못된 경고가 과도하지 않은가?
5.  결과만 보고 다음 작업을 결정할 수 있는가?
6.  데이터가 자동으로 위험하게 수정되지 않았는가?

------------------------------------------------------------------------

# 18. 테스트 기록

두 가지 방식 또는 여러 버전을 실제 데이터로 테스트한 뒤 기록한다.

``` text
[TEST]

파일:
날짜:

잘 탐지한 것:
-

놓친 것:
-

잘못 경고한 것:
-

추천이 도움된 것:
-

추천이 부적절한 것:
-

기존 방식 대비 시간:
-

다음 버전 개선사항:
-
```

실패 사례도 중요한 학습 데이터로 남긴다.

------------------------------------------------------------------------

# 19. 개발 Roadmap

### V0.1 --- Inspector

``` text
inspect_file(path)
```

파일 하나의 구조와 품질 진단.

### V0.2 --- Inspector 강화

-   Excel 시트 진단 강화
-   문자열 이상
-   날짜 후보
-   숫자 범위
-   Key 후보 개선

### V0.3 --- Merge Planner

``` text
compare_files(files)
```

-   컬럼 유사도
-   concat 후보
-   merge 후보
-   Key 관계
-   N:N 위험
-   Grain 불일치 경고

### V0.4 --- Requirement & Recommendation

``` text
recommend(report, requirement)
```

사용자의 목적을 반영하여 작업 순서를 추천.

### V1.0 --- AI Advisor

``` text
Purpose
+
Requirement
+
Inspection Report
+
Merge Report
        ↓
AI Advisor
        ↓
Analysis Pipeline
```

------------------------------------------------------------------------

# 20. 프로젝트 핵심 원칙

> **자동화의 목적은 분석가의 판단을 제거하는 것이 아니라, 반복적인 구조
> 파악을 자동화하여 분석가가 데이터의 의미·결합·요구사항·해석에 더 많은
> 시간을 사용하도록 하는 것이다.**

그리고 분석 과정은 다음 질문으로 시작한다.

``` text
무슨 함수를 사용하지?
```

가 아니라:

``` text
무엇을 알고 싶은가?
↓
최종 DataFrame의 한 행은 무엇이어야 하는가?
↓
어떤 데이터가 필요한가?
↓
현재 데이터의 Grain은 무엇인가?
↓
어떤 Key로 연결할 수 있는가?
↓
어떤 품질 문제가 있는가?
↓
어떤 변환과 결합이 필요한가?
↓
그 작업에 어떤 Pandas 함수를 사용할 것인가?
```

------------------------------------------------------------------------

# 21. 한 문장 정의

> **원천 데이터의 구조와 품질을 자동 진단하고 사용자의 분석 요구사항을
> 구체화하며, 데이터 결합 위험성을 사전에 검증하여 최소한의 AI 입력으로
> 신뢰도 높은 분석 전략을 설계하도록 지원하는 데이터 분석 보조 도구.**

------------------------------------------------------------------------

## 승인 상태

**V0.1 설계 승인**

다음 단계:

``` text
V0.1 기능 명세 고정
↓
inspect_file() 구현
↓
실제 CSV / XLSX 테스트
↓
정상 탐지 / 누락 / 오탐 기록
↓
V0.2 개선
```
